import type { Metadata } from "next";
import { CasePlaceholder } from "@/components/CasePlaceholder";

export const metadata: Metadata = {
  title: "FABIL NATURAL",
};

export default function FabilNaturalPage() {
  return (
    <CasePlaceholder
      title="FABIL NATURAL"
      meta="Brand Content / Product Campaign / Visual Direction"
    />
  );
}
